# Review Guidance

Review document changes against the current layer boundary:

- `isomorph` owns semantic authority and lint vocabulary.
- `docwarden` owns document-system maintenance.
- `contexta` owns agent context packaging and export.

Call out any change that blurs these responsibilities.

---
status: active
updated: 2026-06-04T00:00:00.000Z
---

# Initialized isomorph project workspace

This workspace is initialized from the pinned isomorph package source.

- Keep project-specific semantic material under this `.isomorph` tree.
- The isomorph CLI loads portable basis material from the pinned package source.
- The isomorph CLI loads bootstrap material from the pinned package source.
- Package-owned source material does not belong in this `.isomorph` tree.
